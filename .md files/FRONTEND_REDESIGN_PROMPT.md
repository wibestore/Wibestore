# 🚀 ЗАДАНИЕ: ПОЛНАЯ ПЕРЕРАБОТКА FRONTEND — ULTRA-PREMIUM ИНТЕРФЕЙС WIBESTORE

## 📋 ОБЩЕЕ ОПИСАНИЕ ЗАДАЧИ

**WibeStore** — маркетплейс игровых аккаунтов, требующий **полной переработки frontend-части** с нуля. Текущий интерфейс выглядит непрофессионально, "развалено", визуально некрасиво и не соответствует уровню premium-продукта.

**Цель:** Создать **ультра-премиальный, современный, безупречно аккуратный интерфейс** в двух темах:
- **GitHub Dark Premium** (глубокий тёмный с изысканными акцентами)
- **GitHub Light Premium** (чистый светлый с мягкими контрастами)

---

## 🎯 КРИТИЧЕСКИЕ ПРОБЛЕМЫ ТЕКУЩЕГО ИНТЕРФЕЙСА

### Визуальные проблемы:

1. **Цветовая палитра**
   - Дешёвые градиенты (purple/pink) без дизайнерской ценности
   - Чрезмерное использование ярких цветов без иерархии
   - Отсутствие единой системы нейтральных оттенков
   - Случайные цвета для одинаковых элементов

2. **Типографика**
   - Непоследовательные размеры шрифтов
   - Отсутствует вертикальный ритм
   - Плохой контраст текста
   - Неправильные межстрочные интервалы
   - Хаотичное использование font-weight

3. **Отступы и сетка**
   - Элементы "прыгают" по странице
   - Отсутствует единая система отступов
   - Контейнеры разной ширины без логики
   - Карточки и блоки не выровнены по сетке

4. **Компоненты**
   - Кнопки разных стилей без системы
   - Инпуты без единого дизайна
   - Таблицы без продуманной структуры
   - Модалки выглядят как временное решение
   - Карточки с разной высотой и отступами

5. **Состояния**
   - Отсутствуют hover/active/focus состояния
   - Нет loading/skeleton состояний
   - Empty states выглядят пусто (буквально)
   - Error states отсутствуют
   - Focus-ring для доступности нет

6. **Адаптивность**
   - Мобильная версия работает с проблемами
   - Таблицы ломаются на маленьких экранах
   - Сайдбар не адаптируется корректно
   - Горизонтальный скролл появляется случайно

7. **Навигация**
   - Структура страниц нелогична
   - Хлебные крошки отсутствуют
   - Поиск и фильтры выглядят примитивно
   - Пагинация без дизайна

---

## 🎨 ТРЕБОВАНИЯ К РЕЗУЛЬТАТУ

### 1. ЕДИНАЯ ДИЗАЙН-СИСТЕМА

#### Цветовая палитра

**GitHub Dark Premium:**
```
Основные:
- bg-primary: #0d1117 (основной фон)
- bg-secondary: #161b22 (вторичный фон)
- bg-tertiary: #21262d (третичный фон)
- bg-elevated: #30363d (приподнятые элементы)

Границы:
- border-default: #30363d
- border-muted: #21262d
- border-accent: #1f6feb

Текст:
- text-primary: #f0f6fc
- text-secondary: #8b949e
- text-muted: #484f58
- text-accent: #58a6ff

Акценты:
- accent-blue: #1f6feb
- accent-green: #238636
- accent-orange: #9d6328
- accent-red: #da3633
- accent-purple: #8957e5
- accent-pink: #db6d9e

Премиум:
- premium-gold: #d29922
- premium-gold-light: #f0b724

Состояния:
- success: #238636
- warning: #9e6a03
- error: #da3633
- info: #1f6feb
```

**GitHub Light Premium:**
```
Основные:
- bg-primary: #ffffff (основной фон)
- bg-secondary: #f6f8fa (вторичный фон)
- bg-tertiary: #eaeef2 (третичный фон)
- bg-elevated: #ffffff (приподнятые элементы)

Границы:
- border-default: #d0d7de
- border-muted: #eaeef2
- border-accent: #0969da

Текст:
- text-primary: #1f2328
- text-secondary: #656d76
- text-muted: #8c959f
- text-accent: #0969da

Акценты:
- accent-blue: #0969da
- accent-green: #1a7f37
- accent-orange: #bc6c19
- accent-red: #cf222e
- accent-purple: #8250df
- accent-pink: #ce2c85

Премиум:
- premium-gold: #9a6700
- premium-gold-light: #d4a72c

Состояния:
- success: #1a7f37
- warning: #9a6700
- error: #cf222e
- info: #0969da
```

#### Типографика

**Шрифт:** `-apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans", Helvetica, Arial, sans-serif`

**Размеры и интервалы:**
```
display: 40px / line-height 48px / weight 700
h1: 32px / line-height 40px / weight 600
h2: 24px / line-height 32px / weight 600
h3: 20px / line-height 28px / weight 600
h4: 16px / line-height 24px / weight 600
body: 14px / line-height 20px / weight 400
small: 12px / line-height 16px / weight 400
caption: 11px / line-height 14px / weight 400

font-weights:
- regular: 400
- medium: 500
- semibold: 600
- bold: 700
```

**Вертикальный ритм:**
- Базовый шаг: 4px
- Все отступы кратны 4px
- Основные значения: 4, 8, 12, 16, 20, 24, 32, 40, 48, 64px

#### Радиусы скругления

```
none: 0
small: 4px (кнопки, бейджи)
medium: 6px (инпуты, карточки)
large: 8px (модалки, dropdown)
xlarge: 12px (крупные карточки)
full: 9999px (аватары, pill buttons)
```

#### Тени

**Light:**
```
shadow-xs: 0 1px 2px rgba(65, 76, 86, 0.1)
shadow-sm: 0 1px 3px rgba(65, 76, 86, 0.12)
shadow-md: 0 4px 12px rgba(65, 76, 86, 0.15)
shadow-lg: 0 8px 24px rgba(65, 76, 86, 0.18)
shadow-xl: 0 16px 48px rgba(65, 76, 86, 0.2)
```

**Dark:**
```
shadow-xs: 0 1px 2px rgba(0, 0, 0, 0.4)
shadow-sm: 0 1px 3px rgba(0, 0, 0, 0.5)
shadow-md: 0 4px 12px rgba(0, 0, 0, 0.5)
shadow-lg: 0 8px 24px rgba(0, 0, 0, 0.55)
shadow-xl: 0 16px 48px rgba(0, 0, 0, 0.6)
```

#### Иконки

- **Lucide React** (основной набор)
- Размер: 16px, 20px, 24px
- Stroke width: 1.5px (более тонкие для премиум вида)
- Консистентные отступы: 2px от края контейнера

---

### 2. СТИЛЬ GITHUB PREMIUM

#### Принципы дизайна:

1. **Минимализм**
   - Ничего лишнего
   - Каждый элемент на своём месте
   - Воздух между элементами

2. **Строгость**
   - Без декоративных градиентов
   - Без анимаций ради анимаций
   - Только функциональные эффекты

3. **Контраст**
   - Чёткая иерархия через контраст
   - Акценты только там где нужно
   - Читабельность на первом месте

4. **Консистентность**
   - Одинаковые элементы выглядят одинаково
   - Предсказуемое поведение
   - Единые правила для всех компонентов

5. **Доступность**
   - Контраст текста минимум 4.5:1
   - Focus-ring для всех интерактивных элементов
   - ARIA-атрибуты
   - Keyboard navigation

---

### 3. КОМПОНЕНТЫ И СОСТОЯНИЯ

#### Кнопки

**Размеры:**
```
small: 28px height / 12px padding / 12px font
medium: 32px height / 16px padding / 14px font
large: 40px height / 20px padding / 16px font
```

**Варианты:**
```
primary: accent-blue фон, белый текст
secondary: bg-secondary фон, text-primary текст, border
ghost: прозрачный, hover:bg-secondary
danger: accent-red фон, белый текст
premium: premium-gold фон, белый текст
```

**Состояния (ОБЯЗАТЕЛЬНО ВСЕ):**
```
- default: базовое состояние
- hover: легкое изменение фона/границы
- active: вжатое состояние (transform: scale(0.98))
- focus: focus-ring 2px accent-blue/30
- disabled: opacity 0.5, cursor not-allowed
- loading: spinner + disabled
```

#### Инпуты

**Размеры:**
```
small: 28px height / 8px padding-x / 12px font
medium: 32px height / 12px padding-x / 14px font
large: 40px height / 16px padding-x / 16px font
```

**Состояния:**
```
- default: border-default фон
- hover: border-muted
- focus: border-accent + focus-ring
- error: border-error + error message
- disabled: bg-tertiary, cursor not-allowed
- with-icon: padding-left для иконки
```

**Элементы внутри:**
- Label сверху (12px, semibold, text-secondary)
- Placeholder (text-muted)
- Helper text снизу (12px, text-muted)
- Error message снизу (12px, text-error)
- Иконка слева или справа

#### Карточки

**Структура:**
```
- Header (border-bottom, padding 16-20px)
- Content (padding 16-20px)
- Footer опционально (border-top, padding 16-20px)
```

**Варианты:**
```
- default: bg-secondary, border-default
- elevated: bg-primary, shadow-md
- interactive: hover:border-accent, cursor pointer
- selected: border-accent, bg-accent/10
```

#### Таблицы

**Требования:**
```
- Липкий header (position: sticky)
- Чередование строк (zebra striping опционально)
- Hover эффект на строках
- Выравнивание: текст left, числа right, иконки center
- Плотность: compact (32px), normal (40px), comfortable (48px)
- Пагинация снизу
- Сортировка по клику на header
- Чекбоксы для массовых действий
```

**Структура:**
```
- Table header: bg-tertiary, text-secondary, 12px, semibold
- Table row: border-bottom, 14px font
- Table cell: padding 12-16px
- Last row: no border-bottom
```

#### Модалки

**Структура:**
```
- Overlay: black/60 backdrop-blur
- Container: max-width 500px, bg-primary, border, shadow-xl
- Header: padding 20px, border-bottom, close button
- Content: padding 20px
- Footer: padding 20px, border-top, actions right
```

**Размеры:**
```
small: 400px max-width
medium: 500px max-width
large: 700px max-width
full: 90vw max-width
```

**Анимации:**
```
- fade-in: overlay 0.2s
- slide-up: modal 0.3s cubic-bezier(0.16, 1, 0.3, 1)
```

#### Уведомления (Toast)

**Позиции:**
```
- top-right (default)
- top-center
- bottom-right
- bottom-center
```

**Типы:**
```
- info: accent-blue
- success: accent-green
- warning: accent-orange
- error: accent-red
```

**Структура:**
```
- Icon слева
- Title (semibold)
- Message (regular)
- Close button справа
- Progress bar снизу (auto-close)
```

**Поведение:**
```
- Auto-close 5 секунд
- Stack до 3 уведомлений
- Swipe to dismiss (mobile)
- Click to expand
```

#### Skeleton Loading

**Для карточек:**
```
- Заголовок: 16px height, 60% width
- Описание: 12px height, 100% width, 2-3 строки
- Изображение: aspect-ratio placeholder
- Footer: кнопка 32px height
```

**Анимация:**
```
- shimmer: градиент слева-направо 1.5s infinite
- pulse: opacity 0.5 → 1, 2s infinite
```

#### Empty States

**Структура:**
```
- Icon/illustration (64-96px)
- Title (16-20px, semibold)
- Description (14px, text-secondary, max 300px)
- CTA button опционально
```

**Примеры:**
```
- Нет товаров: корзина + "Корзина пуста"
- Нет уведомлений: колокольчик + "Нет новых уведомлений"
- Нет результатов: лупа + "Ничего не найдено"
```

#### Dropdown / Select

**Структура:**
```
- Trigger: кнопка с chevron
- Menu: bg-primary, border, shadow-lg
- Items: padding 8-12px, hover:bg-secondary
- Divider: border-top между группами
- Checkmark для выбранных
```

**Поведение:**
```
- Открытие по клику
- Закрытие по клику вне
- Keyboard navigation (стрелки, enter, escape)
- Поиск по items (опционально)
```

---

### 4. НАВИГАЦИЯ И UX

#### Структура навигации

**Основная навигация (Top Bar):**
```
- Логотип слева
- Навигация по центру (Products, Top, Statistics, Premium)
- Действия справа (поиск, уведомления, профиль)
- Высота: 64px
```

**Боковая навигация (Sidebar) для админки:**
```
- Логотип сверху
- Группы меню с заголовками
- Активный пункт выделен
- Свёртывание на мобильных
- Ширина: 280px раскрытый, 64px свёрнутый
```

**Хлебные крошки:**
```
- Показывать на всех вложенных страницах
- Формат: Home / Category / Current
- Клик на каждый элемент кроме текущего
- Высота: 40px
```

#### Поиск

**Требования:**
```
- Global search в хедере
- Quick results dropdown
- Recent searches
- Search filters
- Keyboard shortcut (Ctrl/Cmd + K)
```

**UI:**
```
- Инпут с иконкой поиска
- Placeholder "Search..."
- Hotkey hint справа
- Результаты с категориями
```

#### Фильтры и сортировка

**Фильтры:**
```
- Sidebar для десктопа
- Drawer для мобильных
- Активные фильтры сверху (chips)
- Clear all кнопка
- Счётчик результатов
```

**Сортировка:**
```
- Dropdown с иконкой
- Направление (asc/desc)
- Сохранение в URL
```

#### Пагинация

**Варианты:**
```
- Numbered: [1] 2 3 ... 10
- Simple: Previous / Next
- Load more: кнопка снизу
- Infinite scroll: опционально
```

**Требования:**
```
- Текущая страница выделена
- Disabled состояния для границ
- Total count справа
- Items per page selector
```

---

### 5. АДАПТИВНОСТЬ

#### Breakpoints

```
mobile: 0-639px
tablet: 640-1023px
desktop: 1024-1439px
large-desktop: 1440px+
```

#### Принципы

1. **Mobile First**
   - Базовые стили для мобильных
   - Media queries для tablet+ и desktop+

2. **Контейнеры**
   ```
   mobile: 100% - 32px padding
   tablet: 100% - 48px padding
   desktop: max-width 1024px, centered
   large: max-width 1280px, centered
   ```

3. **Сетки**
   ```
   mobile: 1 колонка
   tablet: 2 колонки
   desktop: 3-4 колонки
   large: 4-5 колонок
   ```

4. **Компоненты**
   - Таблицы → карточки на мобильных
   - Sidebar → drawer на мобильных
   - Dropdown → full-width на мобильных
   - Modals → full-screen на мобильных

---

### 6. СТРАНИЦЫ ДЛЯ ПЕРЕРАБОТКИ

#### Публичные страницы:

1. **Home Page**
   - Hero секция
   - Games grid
   - Premium accounts
   - Recommended accounts
   - Trust section
   - CTA section

2. **Products Page (Каталог)**
   - Filters sidebar
   - Search bar
   - Sort dropdown
   - Accounts grid
   - Pagination

3. **Game Detail Page**
   - Game header
   - Filters bar
   - Accounts grid
   - Related games

4. **Account Detail Page**
   - Images carousel
   - Account info
   - Seller info
   - Purchase card
   - Reviews
   - Related accounts

5. **Top Accounts Page**
   - Premium filter
   - Leaderboard style
   - Sort options

6. **Statistics Page**
   - Charts
   - Key metrics
   - Top sellers
   - Top games

7. **Premium Page**
   - Pricing cards
   - Features comparison
   - Benefits section
   - FAQ

8. **Auth Pages**
   - Login
   - Signup
   - Forgot Password

9. **User Pages**
   - Profile
   - Settings
   - Purchases
   - Favorites
   - My Listings

10. **Sell Page**
    - Multi-step form
    - Preview
    - Success state

11. **Info Pages**
    - Terms
    - FAQ
    - Coins

#### Админ страницы:

1. **Dashboard**
   - Stats cards
   - Recent listings
   - Reports
   - Charts

2. **Users Management**
   - Filters
   - Table
   - Actions

3. **Accounts Management**
   - Moderation queue
   - Approve/Reject
   - View credentials

4. **Reports**
   - List
   - Detail
   - Resolve

5. **Premium Management**
   - Subscribers
   - Plans

6. **Finance**
   - Transactions
   - Payouts

7. **Settings**
   - Site config
   - Commission rates

---

## 🛠️ ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ

### Стек технологий

**Обязательно:**
- React 18+ или Next.js 14+
- TypeScript (строгая типизация)
- TailwindCSS 4+ (через дизайн-токены)
- React Query / SWR (data fetching)
- Zustand / Jotai (state management)

**Рекомендуется:**
- shadcn/ui (база компонентов)
- Radix UI (примитивы)
- Framer Motion (анимации)
- React Hook Form + Zod (формы)
- date-fns (даты)

### Структура проекта

```
src/
├── components/
│   ├── ui/                    # Базовые UI компоненты
│   │   ├── button/
│   │   │   ├── button.tsx
│   │   │   ├── button.styles.ts
│   │   │   └── index.ts
│   │   ├── input/
│   │   ├── card/
│   │   ├── table/
│   │   ├── modal/
│   │   ├── dropdown/
│   │   ├── toast/
│   │   ├── skeleton/
│   │   ├── empty-state/
│   │   └── ...
│   ├── layout/                # Layout компоненты
│   │   ├── header/
│   │   ├── footer/
│   │   ├── sidebar/
│   │   └── container/
│   ├── shared/                # Общие компоненты
│   │   ├── game-card/
│   │   ├── account-card/
│   │   ├── seller-badge/
│   │   └── ...
│   └── forms/                 # Формы
│       ├── form-field/
│       ├── select-field/
│       └── ...
│
├── pages/                     # Страницы
│   ├── home/
│   ├── products/
│   ├── account-detail/
│   ├── auth/
│   ├── profile/
│   └── admin/
│
├── hooks/                     # Custom hooks
│   ├── use-toast/
│   ├── use-modal/
│   ├── use-form/
│   └── ...
│
├── lib/                       # Утилиты
│   ├── api/                   # API client
│   ├── utils/                 # Helpers
│   └── constants/             # Constants
│
├── stores/                    # State management
│   ├── auth-store/
│   ├── ui-store/
│   └── ...
│
├── styles/                    # Глобальные стили
│   ├── tokens.css             # Дизайн-токены
│   ├── global.css             # Глобальные стили
│   └── themes/                # Темы
│       ├── dark.css
│       └── light.css
│
├── types/                     # TypeScript типы
│   ├── api.ts
│   ├── models.ts
│   └── ...
│
└── config/                    # Конфигурация
    ├── site.ts
    └── routes.ts
```

### Дизайн-токены

**Реализация через CSS Custom Properties:**

```css
/* styles/tokens.css */
:root {
  /* Colors */
  --color-bg-primary: #ffffff;
  --color-bg-secondary: #f6f8fa;
  --color-text-primary: #1f2328;
  --color-text-secondary: #656d76;
  --color-border-default: #d0d7de;
  --color-accent-blue: #0969da;
  
  /* Spacing */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 20px;
  --space-6: 24px;
  --space-8: 32px;
  
  /* Typography */
  --font-size-xs: 11px;
  --font-size-sm: 12px;
  --font-size-base: 14px;
  --font-size-lg: 16px;
  --font-size-xl: 20px;
  --font-size-2xl: 24px;
  --font-size-3xl: 32px;
  
  /* Border radius */
  --radius-sm: 4px;
  --radius-md: 6px;
  --radius-lg: 8px;
  --radius-xl: 12px;
  --radius-full: 9999px;
  
  /* Shadows */
  --shadow-xs: 0 1px 2px rgba(65, 76, 86, 0.1);
  --shadow-sm: 0 1px 3px rgba(65, 76, 86, 0.12);
  --shadow-md: 0 4px 12px rgba(65, 76, 86, 0.15);
}

[data-theme="dark"] {
  --color-bg-primary: #0d1117;
  --color-bg-secondary: #161b22;
  --color-text-primary: #f0f6fc;
  /* ... остальные токены для dark темы */
}
```

### Интеграция с Backend API

**API Client:**

```typescript
// lib/api/client.ts
const apiClient = {
  get: <T>(url: string, config?: RequestConfig) => Promise<T>,
  post: <T>(url: string, data: unknown, config?: RequestConfig) => Promise<T>,
  put: <T>(url: string, data: unknown, config?: RequestConfig) => Promise<T>,
  delete: <T>(url: string, config?: RequestConfig) => Promise<T>,
};
```

**React Query hooks:**

```typescript
// hooks/api/use-accounts.ts
export function useAccounts(filters: AccountFilters) {
  return useQuery({
    queryKey: ['accounts', filters],
    queryFn: () => apiClient.get('/api/accounts', { params: filters }),
    staleTime: 5 * 60 * 1000,
  });
}
```

**Обработка ошибок:**

```typescript
// lib/api/errors.ts
export class ApiError extends Error {
  constructor(
    public status: number,
    public code: string,
    public message: string,
    public details?: Record<string, string[]>
  ) {
    super(message);
  }
}

// Global error handler
queryClient.setDefaultOptions({
  queries: {
    retry: (failureCount, error) => {
      if (error.status >= 400 && error.status < 500) return false;
      return failureCount < 3;
    },
    onError: (error) => {
      toast.error(error.message);
    },
  },
});
```

---

## 📋 ПЛАН РАБОТЫ ПО ШАГАМ

### Этап 1: Аудит и анализ (1-2 дня)

**Задачи:**
1. Полностью проанализировать текущий frontend
2. Выявить все визуальные проблемы
3. Составить список несогласованностей
4. Зафиксировать хаос в стилях
5. Определить проблемные компоненты

**Результат:**
- Документ с проблемами
- Скриншоты проблемных мест
- Список приоритетов

### Этап 2: Дизайн-система (3-4 дня)

**Задачи:**
1. Определить цветовую палитру для двух тем
2. Создать токены (цвета, отступы, типографика, радиусы, тени)
3. Настроить тему через CSS Custom Properties
4. Создать файл с константами
5. Настроить переключатель тем

**Результат:**
- Файл tokens.css
- Файл themes/dark.css
- Файл themes/light.css
- Хук useTheme()
- Переключатель тем в UI

### Этап 3: Библиотека UI компонентов (5-7 дней)

**Задачи:**
1. Button (все варианты и состояния)
2. Input (все типы и состояния)
3. Card (все варианты)
4. Table (с сортировкой, пагинацией)
5. Modal (с анимациями)
6. Dropdown / Select
7. Toast notifications
8. Skeleton loading
9. Empty states
10. Badge / Tag
11. Avatar
12. Tabs
13. Pagination
14. Breadcrumbs

**Результат:**
- Папка components/ui с 14+ компонентами
- Storybook для документации (опционально)
- Все состояния реализованы
- Accessibility проверен

### Этап 4: Layout компоненты (2-3 дня)

**Задачи:**
1. Header (с поиском, уведомлениями, профилем)
2. Footer
3. Sidebar (для админки)
4. Container
5. PageHeader с breadcrumbs
6. ResponsiveGrid

**Результат:**
- Готовые layout компоненты
- Адаптивность работает
- Консистентные отступы

### Этап 5: Пересборка страниц (7-10 дней)

**Задачи:**
1. Home Page
2. Products Page
3. Game Detail Page
4. Account Detail Page
5. Top Accounts Page
6. Statistics Page
7. Premium Page
8. Auth Pages (Login, Signup, Forgot)
9. Profile Pages
10. Settings Page
11. Sell Page (multi-step form)
12. Info Pages (Terms, FAQ, Coins)
13. Admin Dashboard
14. Admin Users
15. Admin Accounts
16. Admin Reports

**Результат:**
- Все страницы переработаны
- Единый стиль
- Идеальная типографика
- Правильные отступы

### Этап 6: Интеграция с API (3-4 дня)

**Задачи:**
1. Настроить API client
2. Создать React Query hooks
3. Обработка loading состояний
4. Обработка error состояний
5. Кэширование данных
6. Оптимистичные обновления

**Результат:**
- Все данные загружаются через API
- Skeleton loading везде
- Error handling
- Retry logic

### Этап 7: Полировка и UX (3-4 дня)

**Задачи:**
1. Добавить focus-ring для доступности
2. Настроить keyboard navigation
3. Добавить ARIA-атрибуты
4. Проверить контраст текста
5. Оптимизировать анимации
6. Добавить hotkeys (Ctrl+K поиск)
7. Улучшить empty states
8. Добавить confirmation dialogs

**Результат:**
- Доступность на уровне
- Предсказуемый UX
- Приятные микро-взаимодействия

### Этап 8: Тестирование и фиксы (2-3 дня)

**Задачи:**
1. Протестировать на разных экранах
2. Проверить все состояния
3. Исправить визуальные баги
4. Оптимизировать производительность
5. Проверить кроссбраузерность

**Результат:**
- Чеклист качества пройден
- Баги исправлены
- Производительность оптимизирована

---

## ✅ ЧЕКЛИСТ КАЧЕСТВА

### Визуальное качество:

- [ ] Все отступы кратны 4px
- [ ] Типографика консистентна на всех страницах
- [ ] Цвета используются по дизайн-системе
- [ ] Нет случайных градиентов
- [ ] Тени используются консистентно
- [ ] Радиусы скругления одинаковые для одинаковых элементов
- [ ] Иконки одного размера в одинаковых контекстах
- [ ] Нет горизонтального скролла
- [ ] Контейнеры выровнены по центру
- [ ] Карточки одинаковой высоты в grid

### Компоненты:

- [ ] Все кнопки имеют hover/active/focus/disabled/loading
- [ ] Все инпуты имеют hover/focus/error/disabled
- [ ] Таблицы имеют липкий header
- [ ] Таблицы имеют пагинацию
- [ ] Модалки имеют overlay и close
- [ ] Dropdown закрывается по клику вне
- [ ] Toast уведомление auto-close
- [ ] Skeleton loading для всех загрузок
- [ ] Empty states для всех пустых списков

### Адаптивность:

- [ ] Mobile (320px) работает
- [ ] Tablet (768px) работает
- [ ] Desktop (1024px) работает
- [ ] Large Desktop (1440px) работает
- [ ] Sidebar сворачивается на мобильных
- [ ] Таблицы превращаются в карточки на мобильных
- [ ] Модалки full-screen на мобильных
- [ ] Навигация адаптируется

### Доступность:

- [ ] Focus-ring на всех интерактивных элементах
- [ ] Keyboard navigation работает
- [ ] ARIA-атрибуты добавлены
- [ ] Контраст текста минимум 4.5:1
- [ ] Alt text у изображений
- [ ] Labels у форм
- [ ] Error сообщения у инпутов

### Производительность:

- [ ] Lighthouse score > 90
- [ ] First Contentful Paint < 1.5s
- [ ] Time to Interactive < 3.5s
- [ ] Нет layout shift
- [ ] Изображения оптимизированы
- [ ] Код разделён на chunks
- [ ] Ленивая загрузка страниц

### Интеграция:

- [ ] API client настроен
- [ ] React Query hooks созданы
- [ ] Loading состояния везде
- [ ] Error handling работает
- [ ] Retry logic настроен
- [ ] Кэширование работает
- [ ] Оптимистичные обновления

### Код:

- [ ] TypeScript типы для всего
- [ ] Нет any типов
- [ ] Компоненты разделены логически
- [ ] Нет дублирования кода
- [ ] Стили через токены
- [ ] Нет hardcoded значений
- [ ] Код отформатирован
- [ ] Нет console.log в production

---

## 🎯 КРИТЕРИИ ПРИЁМКИ

Проект считается выполненным на 100%, если:

1. ✅ Все 14+ UI компонентов реализованы со всеми состояниями
2. ✅ Все 16+ страниц переработаны
3. ✅ Две темы (Dark/Light) работают идеально
4. ✅ Переключатель тем работает
5. ✅ Адаптивность на всех breakpoint работает
6. ✅ Accessibility проверен (focus-ring, keyboard nav, ARIA)
7. ✅ Все loading states реализованы (skeleton)
8. ✅ Все empty states реализованы
9. ✅ Все error states реализованы
10. ✅ API интеграция работает
11. ✅ Нет визуальных багов
12. ✅ Чеклист качества пройден полностью
13. ✅ Код чистый и типизирован
14. ✅ Производительность оптимизирована
15. ✅ Документация компонентов создана

---

## 💪 ЗАКЛЮЧЕНИЕ

Это задание требует **полной переработки frontend** с созданием **ультра-премиального интерфейса** уровня GitHub Premium. Каждый пиксель, каждый отступ, каждая буква должны быть выверены и соответствовать дизайн-системе.

**Никаких компромиссов.** Никаких "и так сойдёт". Только идеальное исполнение.

Работай как дизайнер уровня Apple + GitHub + Vercel. Думай о каждой детали. Проверяй каждый state. Тестируй на разных экранах.

**Результат должен быть таким, чтобы его хотелось показать на Dribbble.**

**Удачи в создании шедевра! 🚀**
